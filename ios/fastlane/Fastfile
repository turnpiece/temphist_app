# This file contains the fastlane.tools configuration
# You can find the documentation at https://docs.fastlane.tools
#
# For a list of all available actions, check out
#
#     https://docs.fastlane.tools/actions
#
# For a list of all available plugins, check out
#
#     https://docs.fastlane.tools/plugins/available-plugins
#

# Uncomment the line if you want fastlane to automatically update itself
# update_fastlane

default_platform(:ios)

platform :ios do
  desc "Push a new beta build to TestFlight"
  lane :beta do
    setup_ci 

    # Set up API key for App Store Connect
    api_key = app_store_connect_api_key(
      key_id: ENV["APP_STORE_KEY_ID"],
      issuer_id: ENV["APP_STORE_ISSUER_ID"],
      key_content: ENV["APP_STORE_PRIVATE_KEY"],
      is_key_content_base64: true
    )

    # development profile (same bundle ID)
    match(
      api_key: api_key,
      type:    "development",
      readonly: false 
    )

    # Code signing with match
    match(
      api_key: api_key,
      type: "appstore",
    )

    # Build with Flutter
    sh("cd .. && bundle exec flutter build ios --release --no-codesign")

    # Package and sign IPA
    build_app(
      scheme:        "Runner",
      configuration: "Release",
      export_method: "app-store",

      # 🔽 force Xcode to use the App-Store profile that match just installed
      export_options: {
        signingStyle: "manual",
        provisioningProfiles: {
          ENV["APP_IDENTIFIER"] =>
            ENV["MATCH_PROVISIONING_PROFILE_MAPPING"]
              .split("=>").last.strip.tr('"', '')   # "match AppStore …"
        }
      }
    )

    upload_to_testflight(api_key: api_key)
  end
end
